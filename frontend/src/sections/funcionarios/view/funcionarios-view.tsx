import { Container, Typography } from '@mui/material';

// ----------------------------------------------------------------------

export function FuncionariosView() {
    return (
        <Container maxWidth="xl">
            <Typography variant="h4" sx={{ mb: 5 }}>
                Funcionários
            </Typography>

            {/* Página em branco - conteúdo será adicionado posteriormente */}
        </Container>
    );
}
